{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core IPC plus the folder-picker dialog. File reveal and all filesystem mutation go through typed Rust commands, never generic FS permissions.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
